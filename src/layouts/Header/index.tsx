import React from 'react'
import { Layout } from 'antd'
import '../index.less'
const { Header } = Layout
const AppHeader = () => {
  return (
    <Header className='app-header'>
      header
    </Header>
  )
}

export default AppHeader
